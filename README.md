# ViewTube

A static YouTube-style website interface built with plain HTML, CSS, and JavaScript.

Open `index.html` in a browser to view the interface. The page includes a responsive feed, topic chips, search filtering, local thumbnail assets, a shorts strip, and a static watch drawer.

To regenerate the thumbnail PNGs:

```bash
node tools/render-thumbnails.js
```
